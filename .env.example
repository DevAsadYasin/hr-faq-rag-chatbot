# LLM Provider API Keys (set at least one)
# Priority order: OpenRouter → Gemini → OpenAI (configurable via LLM_PRIORITY_ORDER)
OPENROUTER_API_KEY=your-openrouter-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# LLM Priority Order (comma-separated, default: openrouter,gemini,openai)
# User can customize the order: e.g., "gemini,openai,openrouter"
LLM_PRIORITY_ORDER=openrouter,gemini,openai

# Model Selection (optional, defaults provided)
# OpenRouter models: https://openrouter.ai/models
LLM_MODEL_OPENROUTER=openai/gpt-3.5-turbo
# Gemini models: gemini-pro, gemini-1.5-pro, etc.
LLM_MODEL_GEMINI=gemini-pro
# OpenAI models: gpt-3.5-turbo, gpt-4, etc.
LLM_MODEL_OPENAI=gpt-3.5-turbo

# OpenRouter Configuration
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Legacy support (for backward compatibility)
LLM_MODEL=gpt-3.5-turbo
LLM_TEMPERATURE=0

# Embeddings (local by default for confidentiality)
EMBEDDING_MODEL=local
LOCAL_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Document path
FAQ_DOCUMENT_PATH=data

# Other settings...
